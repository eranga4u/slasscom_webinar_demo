server:
  port: 8081
management:
  endpoints:
    web:
      base-path: /
      exposure:
        include:  health
  endpoint:
    metrics:
      enabled: true
spring:
  data:
    mongodb:
      uri: ${MONGO_URL}
      #uri: mongodb://172.17.0.2:27017/demo
     
      